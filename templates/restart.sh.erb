#!/bin/bash

# This script is managed by Puppet

/etc/init.d/openntpd stop
for i in {1..30}; do
  /etc/init.d/openntpd status > /dev/null
  if [ $? -eq 0 ]; then
    echo "Waiting for process to stop"
    sleep 1
  else
    break
  fi
done

/etc/init.d/openntpd start
